import { GetApTotalPipe } from './get-ap-total.pipe';

describe('GetApTotalPipe', () => {
  it('create an instance', () => {
    const pipe = new GetApTotalPipe();
    expect(pipe).toBeTruthy();
  });
});
