diff a/Frontend/src/app/pipes/get-ap-stats/get-ap-stats.pipe.ts b/Frontend/src/app/pipes/get-ap-stats/get-ap-stats.pipe.ts	(rejected hunks)
@@ -41,7 +41,7 @@
 
     if (output.total > 0) {
       output.percent = Math.round((output.points / output.total) * 100) + '%';
-      output.string = `${output.points}/${output.total} (${output.percent})`;
+      output.string = `${Math.round(output.points)}/${output.total} (${output.percent})`;
       output.remainingPercent = Math.round((output.total/(output.total+output.remaining)) * 100) + '%';
     } else {
       output.percent = '0%';
